/* functions (prompt, alert, arow functions), loops, DOM manipulation,conditions(ternary), variables  (const), different data types(objects), m scope, events, class */

const birthYear= 1967;
const products= [];
const product = {};

let anoterYear = prompt("Give me a year");

function addValues(numbers){
    let sum = 0;
    for(let i =0; i<numbers.length; i ++){
        console.log(numbers[i]);
    }

    numbers.forEach(function(number) {
        
    });
    //return num1+num2;
}